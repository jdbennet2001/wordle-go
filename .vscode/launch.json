{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Wordle Go (Fiber)",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}",
      "cwd": "${workspaceFolder}",
      "env": {
        "PORT": "3000"
      }
    }
  ]
}
